'use strict';

var mongoose = require('mongoose');
var updatedTimestamp = require('mongoose-updated_at');

var <%= modelNameSnake %>Schema = new mongoose.Schema({

    mobile:{type:String},
    
    content:{type:Number},
    
    status:{
        type:String,
        enum:['0','1','2'] //0-未发送，1-已发送，2-发送失败
    },

    provider:{
        type:String,
        enum:['CL'] //CL-创兰
    },

    msgId:{type:String},

    remark:{type:String},

    
    updatedAt:Date,
    createdAt:{type:Date,default:new Date()}
}, {
    collection: '<%= modelName.toLowerCase() %>s'
});
<%= modelNameSnake %>Schema.plugin(updatedTimestamp);
module.exports = mongoose.model('<%= modelName %>', <%= modelNameSnake %>Schema);
